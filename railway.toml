[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
buildCommand = "chmod +x ./mvnw && ./mvnw clean package -DskipTests"

[deploy]
startCommand = "java -Dserver.port=$PORT -jar target/satellite-api-0.0.1-SNAPSHOT.jar"
